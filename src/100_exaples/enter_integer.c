#include <stdio.h>

int main() {
    int n;

    // Предлагаем ввести число
    printf("Enter a number: ");
    // Считываем число и записываем его в переменную n
    scanf("%d", &n);

    // Выводим в консоль значение n
    printf("n = %d", n);
    return 0;
}
